FOOTBALL_DATA_API_KEY=your_key_here
# PostgreSQL credentials (must match docker-compose)
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=atom_db

# This is the key line:
DATABASE_URL=postgresql://user:password@db:5432/atom_db

#=== ONE-OFF DESTRUCTIVE FIX FLAG ===
#Set this to 'True' once when you encounter a schema mismatch error
#to force a complete recreation of the database structure.
#DELETE this line or set to 'False' after the first successful run
#to prevent accidental data loss later.

FORCE_DESTROY_DB=False